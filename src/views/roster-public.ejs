<%- include('partials/head') %>
<%- include('partials/nav') %>

<div class="hero-section">
  <div class="container">
    <h1>🏢 Roster del Equipo</h1>
    <p class="lead">Conoce a los integrantes del equipo</p>
  </div>
</div>

<div class="container mt-5 mb-5">
  
  <div class="card card-custom mb-5">
    <div class="card-body d-flex align-items-center">
      <img src="<%= equipo.logo_url || 'https://via.placeholder.com/150' %>" 
           style="width: 80px; height: 80px; object-fit: cover;" 
           class="rounded-circle me-4" style="border: 4px solid var(--color-amarillo);">
      <div class="flex-grow-1">
        <h2 style="color: var(--color-amarillo); margin-bottom: 0.5rem;"><%= equipo.nombre %></h2>
        <span class="badge" style="background-color: var(--color-rojo-oscuro); color: var(--color-amarillo);"><%= equipo.region %></span>
      </div>
      <div class="ms-auto">
        <a href="/equipos" class="btn btn-custom">← Volver a Equipos</a>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card card-custom">
        <div class="card-body p-0">
          <h5 style="color: var(--color-amarillo); padding: 1.5rem 1.5rem 0 1.5rem; margin-bottom: 1.5rem; border-bottom: 2px solid var(--color-rojo-oscuro); padding-bottom: 1rem;">
            Plantilla - <%= equipo.nombre %>
          </h5>
          <div class="table-responsive">
            <table class="table table-hover mb-0 align-middle">
              <thead>
                <tr>
                  <th style="width: 15%;">Rol</th>
                  <th style="width: 15%;">Posición</th>
                  <th style="width: 40%;">Nombre de Invocador</th>
                  <th style="width: 30%;">Rango</th>
                </tr>
              </thead>
              <tbody>
                <% if (jugadores && jugadores.length > 0) { %>
                  <% jugadores.forEach(jugador => { %>
                    <tr>
                      <td>
                        <% if (jugador.tipo_rol === 'TITULAR') { %>
                          <span class="badge" style="background-color: #4caf50; color: white;">Titular</span>
                        <% } else if (jugador.tipo_rol === 'SUPLENTE') { %>
                          <span class="badge" style="background-color: var(--color-amarillo); color: var(--color-rojo-oscuro);">Suplente</span>
                        <% } else if (jugador.tipo_rol === 'COACH') { %>
                          <span class="badge" style="background-color: var(--color-rojo-oscuro); color: var(--color-amarillo);">Coach</span>
                        <% } %>
                      </td>
                      <td>
                        <% if (jugador.rol_juego) { %>
                          <span class="badge" style="background-color: var(--color-rojo-oscuro); color: var(--color-amarillo);"><%= jugador.rol_juego %></span>
                        <% } else { %>
                          <span style="color: var(--color-gris-claro);">—</span>
                        <% } %>
                      </td>
                      <td style="color: var(--color-amarillo); font-weight: 600;"><%= jugador.nombre_invocador %></td>
                      <td style="color: var(--color-gris-claro);"><%= jugador.rango || '—' %></td>
                    </tr>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="4" class="text-center py-5">
                      <p style="color: var(--color-gris-claro); margin-bottom: 0;">Este equipo aún no tiene jugadores registrados</p>
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
