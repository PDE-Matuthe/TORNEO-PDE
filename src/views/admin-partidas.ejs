<%- include('partials/head') %>
<%- include('partials/nav') %>

<div class="container mt-5">
  <div class="row">
    
    <div class="col-md-4">
      <div class="card bg-secondary text-white sticky-top" style="top: 20px; z-index: 1;">
        <div class="card-header bg-primary fw-bold">
          <i class="fas fa-plus-circle"></i> Nueva Partida
        </div>
        <div class="card-body">
          <form action="/admin/partidas/crear" method="POST">
            
            <div class="mb-3">
              <label class="form-label">Fase del Torneo</label>
              <select name="fase" class="form-select">
                <option>Fase de Grupos</option>
                <option>Cuartos de Final</option>
                <option>Semifinal</option>
                <option>Gran Final</option>
              </select>
            </div>

            <div class="mb-3">
              <label class="form-label text-info">Equipo Azul (Lado Izq)</label>
              <select name="equipo_azul" class="form-select" required>
                <% equipos.forEach(eq => { %>
                  <option value="<%= eq.id %>"><%= eq.nombre %></option>
                <% }) %>
              </select>
            </div>

            <div class="mb-3">
              <label class="form-label text-danger">Equipo Rojo (Lado Der)</label>
              <select name="equipo_rojo" class="form-select" required>
                <% equipos.forEach(eq => { %>
                  <option value="<%= eq.id %>"><%= eq.nombre %></option>
                <% }) %>
              </select>
            </div>

            <div class="row">
              <div class="col-6 mb-3">
                <label class="form-label">Fecha</label>
                <input type="date" name="fecha" class="form-control" required>
              </div>
              <div class="col-6 mb-3">
                <label class="form-label">Hora</label>
                <input type="time" name="hora" class="form-control" required>
              </div>
            </div>

            <button type="submit" class="btn btn-light w-100 fw-bold">Programar Enfrentamiento</button>
          </form>
        </div>
      </div>
    </div>

    <div class="col-md-8">
      <h3 class="text-white mb-3">Gestionar Resultados</h3>
      <div class="alert alert-info">
        Para ver la lista completa y definir ganadores, ve al <a href="/calendario" class="fw-bold">Calendario Público</a>. 
        <br>
        (Desde allí podrás ver cómo quedaron).
      </div>
      
      </div>
  </div>
</div>

<%- include('partials/footer') %>